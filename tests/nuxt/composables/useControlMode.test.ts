import { describe, it } from 'vitest'

describe('useControlMode', () => {
  it.todo('detects touch device via matchMedia')
  it.todo('toggles between keyboard and touch mode')
  it.todo('defaults to keyboard on desktop')
})
