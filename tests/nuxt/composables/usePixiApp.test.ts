import { describe, it } from 'vitest'

describe('usePixiApp', () => {
  it.todo('initializes PixiJS Application with canvas element')
  it.todo('disposes application on unmount')
  it.todo('resizes canvas on container resize')
})
